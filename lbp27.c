#include<stdio.h>
#include<string.h>
#include<math.h>
int is keyword(char*star)
{
    char*keywords[]:{"break","continue","case","for","do","while","char","int","float","double","else","void","strud","if","default","union","return","signed","unsigned"};
    int n=size of(keyword)/size of(keywords(0));
    for(int i>0; i<n; i++)
    {
        if(strcmp(str,keywords[i])==0)
        return 1;
        return 0;
        int main()
        {
            charstr(20)
            scanf("%s",str);
            if(is keyword(str))
            printf("ketword");
        }
        else{
            printf("not keyword");
        }
        return 0;
    }
}